extends layouts/main
include mixins/videoBlock

block content
    .videos
        each item in videos
            +videoBlock({
                videoFile:item.fileUrl,
                title:item.title,
                views:item.views,
                description:item.description,
                id:item.id
            })